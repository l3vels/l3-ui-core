export enum DotStates {
  ACCENT = "accent",
  POSITIVE = "positive",
  WARNING = "warning",
  NEGATIVE = "negative"
}

export enum BadgeType {
  PRIMARY = "primary",
  PRIMARYCIRCLE = "primaryCircle",
  SECONDARY = "secondary",
  TERTIARY = "tertiary"
}

export enum DraftShape {
  WARNING = "warning",
  POSITIVE = "positive",
  NEGATIVE = "negative"
}

export enum SpecialType {
  WITHTEXT = "withText",
  WITHOUTTEXT = "withoutText"
}
